export const getTextColour = () => {
  return ["white", "gray"]
}

export const getBckCol = () => {
  return ["transparent", "transparent"]
}

export const bgColor = { light: "gray.50", dark: "#1B1A17" }

export const color = { light: "black", dark: "white" }
